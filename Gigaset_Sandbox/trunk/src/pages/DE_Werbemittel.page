<apex:page showheader="false" id="de_werbemittel" sidebar="false" controller="DETemplateController"  
	title="{!$Label.de_title}" language="{!defaultlanguage}" action="{!init}" 
	cache="true" expires="600"	>

<apex:composition template="{!$Site.Template}">

        <apex:define name="body">
                <!--  <div id="prodOver"> -->
                    <!--Start LeftSide Content -->
                <!-- <div class="contentText"> -->
                        <div class="contactBox">
                            <div id="contact">
                                <c:DE_werbemittel rendered="{!(isAuthenticated && NOT(isDone))}" id="werbemittelcomp" />
                                <c:DE_werbemittelFinal rendered="{!(isAuthenticated && isDone)}" id="werbemittelcomp_finalcomp" />
                            </div>
                            <span id="end" />
                        </div>
                    <!--  </div> -->
                    <!--End LeftSide Content -->
                
                    <!--Start RightSide Content -->
                    <div id="colRgt">
                        <c:DEcontactportlet rendered="{!AND(isAuthenticated,NOT(showclearingcomp),showcontactportlet)}" />
         				<c:DEuserprofileportlet rendered="{!AND(isAuthenticated,NOT(showclearingcomp))}" />
                        
                        <!-- <img class="lft" src="{!URLFOR($Resource.gigasetciZip, 'img/pic_cont_04.jpg')}" alt="hotline" /> -->
                        <apex:outputPanel id="rightcolumn" rendered="{!NOT(isAuthenticated)}">
                        <!--Start oneColInfo -->
                        <div class="oneCol">
                            <div class="oneColInfo">
                                <c:contentblock name="rghtcol1" />
                            </div>
                            <div class="oneColBtm">
                                <c:contentblock name="rghtcol1Btm" />
                            </div>
                        </div>
                        <!--End oneColInfo --> 
                        <!--Start oneColInfo -->
                        <div class="oneCol">
                            <div class="oneColInfo">
                                <c:contentblock name="rghtcol2" />
                            </div>
                            <div class="oneColBtm">
                                <c:contentblock name="rghtcol2Btm" />
                            </div>
                        </div>
                        <!--End oneColInfo -->
                        </apex:outputPanel>
                    </div> 
                    <!--End RightSide Content --> 
                <!--  </div> -->
                <!-- end prodOver -->   
    </apex:define>
</apex:composition>

</apex:page>